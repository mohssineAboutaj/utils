const {
  removeTwins,
  removeFromArray,
  removeDeplicates,
} = require("./src/remove");
const { merge } = require("./src/merge");
const { toggleArrayValue } = require("./src/toggle");

module.exports = {
  removeTwins,
  removeDeplicates,
  merge,
  removeFromArray,
  toggleArrayValue,
};
